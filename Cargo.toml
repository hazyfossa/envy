[package]
name = "envy"
version = "0.1.0"
edition = "2024"

[dependencies]
snafu = "0.9.0"
tokio = { version = "1", default-features = false, features = [
    "process",
], optional = true }

[features]
tokio = ["dep:tokio"]
